<h1 class="display-4 p-2">Cities</h1>

<table class="table table-striped">
  <thead class="thead-inverse">
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Country</th>
      <th>Photo</th>
      </tr>
  </thead>
  <tbody>
    <% @cities.each do |c| %>
      <tr>
        <td><%= c.id %></td>
        <td><%= c.name %></td>
        <td><%= c.country %></td>
        <td><%= image_tag(c.photo, width: 75, height: 75, class: 'img-thumbnail') %></td>
        </tr>
      <% end %>
  </tbody>
</table>
